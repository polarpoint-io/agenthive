apiVersion: v1
kind: Service
metadata:
  name: {{ include "agenthive.fullname" . }}
  labels:
    {{- include "agenthive.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agenthive.selectorLabels" . | nindent 4 }}
